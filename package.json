{
  "name": "sqlize-query",
  "version": "1.4.1",
  "description": "Converts sequelize queries from query params to sequelize query object ",
  "main": "sqlize.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "sudiq/sqlize-query",
  "keywords": [
    "sequelize",
    "sql",
    "typescript"
  ],
  "license": "ISC",
  "peerDependencies": {
    "sequelize": "^6.6.2"
  }
}
